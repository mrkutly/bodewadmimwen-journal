type Word {
  word: String!
  translation: String!
  type: WordType!
  notes: [Note!]! @relation
}

type Note {
  title: String!
  content: String!
  word: Word!
}

enum WordType {
  VERB
  NOUN
  PHRASE
}

type Query {
  words: [Word!]!
}
